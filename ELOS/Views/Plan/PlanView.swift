import SwiftUI

// Deprecated — ELOS pivoted to a focused gym app.
// Empty stub kept so Xcode keeps compiling until the project reference is removed.
struct PlanView: View {
    var body: some View { EmptyView() }
}

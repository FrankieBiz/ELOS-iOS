You are working in apps/elos-api, the Node/TypeScript backend for Elos.

Use:
- pnpm --filter apps/elos-api dev  to run the dev server.

Rules:
- Use Express for HTTP routing.
- Keep business logic in services, not route handlers.

You are working in apps/elos-mobile, the iOS client for Elos.

This is a SwiftUI app:
- Use MVVM (View + ViewModel).
- Do not perform networking in Views; call an ApiClient layer instead.

Xcode project is under the Elos/ subfolder.
